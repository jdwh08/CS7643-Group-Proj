name: cs7643-flood
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - rasterio
  - numpy
  - matplotlib
  - jupyterlab
  - notebook
  - ipykernel
  - scipy
  - pip
  - pip:
      - albumentations
      - torch
      - torchvision
      - terratorch
      - transformers
      - evaluate
